# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/web/node_modules
/node_modules
/.pnp
.pnp.*

# next.js
/web/.next/
/web/out/

# production
/web/build

# python / engine
/engine/.venv
__pycache__/
*.pyc

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but the templates (no secrets, only var names) are committed on purpose.
!.env.example
!web/.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
web/next-env.d.ts

# test artifacts
.playwright-mcp/

# reference implementation (kept locally, not part of the build)
"CompBrain Company Website"/

# uploaded files (dev)
/web/.storage/
